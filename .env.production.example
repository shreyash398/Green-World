# Production Environment Variables for GreenWorld Platform
# Copy these values to a new .env file on your Hostinger server

# Port (Hostinger usually sets this automatically, but 8080 is a good default)
PORT=8080

# Security - CHANGE THIS to a long, random string!
JWT_SECRET=your_super_secret_production_key_here

# Mode
NODE_ENV=production

# Database (Keep this as greenworld.db for SQLite)
DB_PATH=greenworld.db

# Optional: Custom Ping Message
PING_MESSAGE=Greenworld Platform Live
